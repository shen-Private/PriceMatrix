# 📋 日誌 2026-03-03（補）

## 今日內容

開發日。完成折扣批次修改、Audit Log、庫存警報測試、權限系統（hardcode）、安全庫存前端設定。

---

## 完成項目

### pricing 模組
- 後端：新增 `DiscountAuditLog.java`（Entity）
- 後端：新增 `DiscountAuditLogRepository.java`
- 後端：`DiscountService.java` 加入 audit log 寫入（UPDATE / BATCH_UPDATE / CREATE）
- 後端：`DiscountController.java` 加入：
  - `PUT /api/discounts/batch`（批次修改）
  - `GET /api/discounts/{id}/audit-logs`（變更歷史查詢）
- 前端：`DiscountPanel.tsx` 加入：
  - 批次修改（勾選多筆 → 統一設定折扣%）
  - Audit Log 側欄（點擊 ☰ 展開該筆的變更歷史）

### inventory 模組
- 庫存警報測試：用 SQL 直接修改 quantity 驗證 ⚠ 觸發邏輯，確認正常

### 權限系統
- 新增 `AuthContext.tsx`（全域角色狀態 + 權限判斷）
- 角色定義：admin / warehouse / sales / CS
- 權限表：

| 角色 | 折扣管理 | 庫存總覽 | 掃碼入出庫 | 設定安全庫存 |
|------|----------|----------|------------|--------------|
| admin | ✅ | ✅ | ✅ | ✅ |
| warehouse | ❌ | ✅ | ✅ | ❌ |
| sales | ✅ | ✅ | ❌ | ❌ |
| CS | ❌ | ✅ | ❌ | ✅ |

- `App.tsx` 加入角色選擇畫面 + 權限保護路由
- `Header.tsx` 依角色顯示導航項目 + 右上角顯示目前角色 / 切換身份
- `InventoryPanel.tsx` 加入安全庫存 inline 編輯（只有 `set_safety_stock` 權限才顯示欄位）

---

## 踩到的坑

### 無（今日執行順利）

---

## 現況確認

| 功能 | 狀態 |
|------|------|
| 折扣批次修改 | ✅ 完成 |
| Audit Log（折扣變更歷史） | ✅ 完成 |
| 庫存警報（⚠ 標記） | ✅ 驗證完成 |
| 權限系統（hardcode） | ✅ 完成 |
| 安全庫存前端設定 | ✅ 完成 |

---

## 啟動卡
```
上次做到：
- DiscountAuditLog（批次修改 + 單筆修改 + 新增都有寫 log）
- PUT /api/discounts/batch（批次修改 API）
- GET /api/discounts/{id}/audit-logs（變更歷史 API）
- AuthContext.tsx（角色：admin / warehouse / sales / CS）
- Header 依角色顯示，右上角可切換身份
- InventoryPanel 安全庫存 inline 編輯（CS / admin 才看得到）

下一步：
- Render 雲端同步（audit log table、前端新功能）← 到期日 2026-03-20
- 登入系統（目前是 hardcode 角色選擇，之後接帳號 + 密碼 + JWT）
- TypeScript 遷移（backlog）
- InventoryPanel 拆分 component（backlog）

待注意：
- Render PostgreSQL 到期日 2026-03-20
- application-local.properties 不要 push
- 本地 psql 連線：
  "C:\Program Files\PostgreSQL\18\bin\psql.exe" -U postgres -d pricematrix
- Render 沒有 auto-deploy，push 後記得手動確認
- 開發日：只帶最新日誌 + 相關檔案
- 對話變長會變慢，適時開新對話
```