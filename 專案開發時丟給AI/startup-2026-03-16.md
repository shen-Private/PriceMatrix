# PriceMatrix — 啟動卡 2026-03-16

## 上次做到

### 今日討論確認（架構規劃）
- `pricing_customers` 補欄位：phone / address / contact_person / note / parent_id / created_at / updated_at
- 多事務所方案：parent_id 自我參照 FK
- 新增客戶權限：sales / admin 可，CS / warehouse 不給
- 頁面結構確認：報價單 / 訂單管理 / 客戶管理（Tab）/ 日報填寫
- 日報欄位：日期 / 客戶 / 互動類型 / 內容 / 結果 / 下次行動
- 全域搜尋列為 Backlog（sales 模組完成後再做）
- DB 遷移方向：Render PostgreSQL → Neon（免費永久）
- 文件更新：DB架構決策-v3.md / 統一平台架構規劃-v3.md

### 上次開發完成
- sales 模組報價單功能完成（後端 + 前端）
- 報價單狀態：DRAFT → SENT（鎖定）→ CONVERTED / CANCELLED
- 修正版本管理：parent_quote_id 追溯來源
- Vercel Proxy 跨域修正（vercel.json rewrite）
- 未知條碼簡易建檔 barcode 修正

---

## 今日目標

1. **Render → Neon DB 遷移**
2. **報價單 繼續開發**

---

## 今日作業順序

### Step 1：Neon DB 遷移
1. Neon 建立新 DB（https://neon.tech）
2. 本地 `pg_dump` 匯出 Render DB
3. 匯入 Neon
4. Render 後端環境變數 `DATABASE_URL` 改為 Neon 連線字串
5. Render 手動重新 deploy
6. 測試 API 正常

### Step 2：報價單繼續開發
優先順序：
- [ ] `createdBy` 從 JWT 取得（目前寫死 "unknown"）
- [ ] 折扣自動帶入（目前只帶 base_price，尚未串 pricing_discounts）
- [ ] 報價單轉訂單（按「確認成立」）

---

## 待注意

- Render PostgreSQL 到期日 **2026-03-20（剩 4 天）⚠ 最優先**
- `application-local.properties` 不要 push
- `.env.local` 不要 push（`.env.production` 可以 push，已改為空字串）
- `git push origin master`（不是 main）
- Render 沒有 auto-deploy，push 後記得手動確認
- 本地開發：後端要先啟動（IntelliJ），前端才能正常連線
- Neon 連線字串：本地用 `jdbc:postgresql://...` 外部 URL，非 Render 內部 URL
