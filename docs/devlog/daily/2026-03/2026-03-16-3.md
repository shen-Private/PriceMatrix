# 📋 日誌 2026-03-16（三）

## 今日內容
客戶管理補欄位・軟刪除・前端客戶管理頁面・訂單管理模組實作。

---

## 完成項目

### 客戶管理

#### DB
- `ALTER TABLE pricing_customers` 補欄位：
  phone / address / contact_person / note / parent_id / created_at / updated_at / is_active

#### 後端
- `Customer.java`：補新欄位、`@PrePersist` / `@PreUpdate`、`parent` 自我參照 FK
- `CustomerService.java`：`createCustomer` / `updateCustomer` / `deactivateCustomer` 新增
- `CustomerRepository.java`：`findByActiveTrue()` 新增
- `CustomerController.java`：`POST` / `PUT` / `PATCH /{id}/deactivate` 新增

#### 前端
- `AuthContext.tsx`：`Action` 新增 `view_customers`，permissions 給 admin / sales
- `CustomerPanel.tsx` 新建：客戶列表、新增/編輯 Modal、上層客戶選擇、軟刪除後端
- `App.tsx` / `Header.tsx`：加入 `/customers` 路由與導航

### 訂單管理

#### DB
```sql
CREATE TABLE sales_order (...)
CREATE TABLE sales_shipment (...)
```

#### 後端
- `SalesOrder.java` / `SalesShipment.java` Entity 建立
- `SalesOrderRepository.java` / `SalesShipmentRepository.java` 建立
- `SalesOrderService.java`：convertFromQuote / getAllOrders / createShipment / confirmShipment
- `SalesOrderController.java`：POST from-quote / GET / POST shipments / PATCH confirm

#### 前端
- `OrderPanel.tsx` 新建：訂單列表、展開式出貨單管理、建立出貨單表單、確認出貨
- `QuotePanel.tsx`：確認成立改為呼叫 `POST /api/orders/from-quote/{quoteId}`
- `App.tsx` / `Header.tsx`：加入 `/orders` 路由與導航

---

## 設計決策
- 客戶軟刪除：`is_active = false`，不硬刪（FK 保護 + 財務記錄保留）
- 訂單與報價單 1對1，分割出貨列入 backlog
- 出貨單全部 SHIPPED → 訂單自動變 COMPLETED
- 運送公司固定選項：ヤマト運輸 / 佐川急便 / 福山通運

---

## 現況確認

| 功能 | 狀態 |
|------|------|
| 客戶補欄位（DB + 後端） | ✅ 完成 |
| 客戶管理前端 | ✅ 完成 |
| 客戶軟刪除後端 | ✅ 完成 |
| 訂單管理（後端 + 前端） | ✅ 完成 |
| 出貨單管理 | ✅ 完成 |
| 客戶停用前端按鈕 | ❌ 未做 |
| 雲端同步 | ❌ 未做 |

---

## 啟動卡
```
上次做到：
- 客戶管理：補欄位、軟刪除、前端新增/編輯頁面
- 訂單管理：報價單轉訂單、出貨單建立與確認出貨
- 運送公司固定選項：ヤマト運輸 / 佐川急便 / 福山通運

下一步：
- 雲端同步（git push + Render 手動 deploy + Neon migration）
  → ALTER TABLE pricing_customers（補欄位 + is_active）
  → CREATE TABLE sales_order / sales_shipment
- 客戶停用按鈕（admin only，前端 CustomerPanel）
- audit log 的 operatedBy 接登入系統

待注意：
- application-local.properties 不要 push
- .env.local 不要 push
- git push origin master（不是 main）
- Render 沒有 auto-deploy，push 後記得手動確認
- 分割出貨已列入 backlog
```