# 📋 日誌 2026-03-29（續）

## 今日內容（下午）

Customer 模組抽離 + 全域搜尋欄

---

## Customer 模組抽離 ✅

### 後端
- 建立獨立 `customer` package（與 auth / inventory / pricing / sales 同層）
- 搬移：`Customer.java` / `CustomerRepository.java` / `CustomerService.java` / `CustomerController.java`
- 使用 IntelliJ 拖移 + Refactor 自動更新所有 import
- `@Table(name = "pricing_customers")` → `@Table(name = "customers")`

### DB
```sql
ALTER TABLE pricing_customers RENAME TO customers;
```
- 本地 + Neon 皆執行完成

### Index 新增
```sql
CREATE INDEX IF NOT EXISTS idx_customers_name ON customers(name);
CREATE INDEX IF NOT EXISTS idx_customers_contact ON customers(contact_person);
CREATE INDEX IF NOT EXISTS idx_customers_email ON customers(email);
CREATE INDEX IF NOT EXISTS idx_customers_phone ON customers(phone);
CREATE INDEX IF NOT EXISTS idx_quotes_id ON sales_quote(id);
CREATE INDEX IF NOT EXISTS idx_products_name ON pricing_products(name);
```

---

## 全域搜尋欄 ✅

### 後端
- 新建 `search` package
- `SearchController.java`：`GET /api/search?q=關鍵字`
- 同時查詢客戶 / 報價單 / 商品，回傳三組結果
- 各 Repository 加 `searchByKeyword`：
  - `CustomerRepository`：name / contactPerson / email / phone
  - `SalesQuoteRepository`：id
  - `ProductRepository`：name

### 前端
- 新建 `GlobalSearch.tsx` + `GlobalSearch.module.css`
- 安裝 `use-debounce`，輸入停止 300ms 後才發 API
- 下拉清單分 Tab：全部 / 客戶 / 報價單 / 商品
- 點選結果跳轉對應頁面，URL 帶 `?q=關鍵字`
- `CustomerPanel` / `QuotePanel` / `InventoryPanel` / `OrderPanel` 接收 `useSearchParams` 自動篩選

### 修正
- 商品跳轉路徑：`/pricing` → `/inventory`
- 報價單跳轉路徑：`/sales` → `/quotes`

---

## 雲端同步
- 前後端 git push 完成
- Render deploy 待確認
- Neon index migration 完成

---

## 啟動卡

\```
上次做到：
- Customer 模組從 pricing 抽離為獨立模組
- DB 表名 pricing_customers → customers
- 全域搜尋欄完成（Header 搜尋框 + 各頁面 URL 篩選）

下一步：
- Render deploy 確認
- PDF 報價單輸出（Node.js + Puppeteer，待規劃部署方式）

待注意：
- application-local.properties 不要 push
- .env.local 不要 push
- git push origin master（不是 main）
- Render 沒有 auto-deploy，push 後記得手動確認
\```