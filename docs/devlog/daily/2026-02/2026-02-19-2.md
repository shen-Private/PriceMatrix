📋 今日日誌（2026-02-19 下午）
完成功能：

psql 安裝（PostgreSQL 18）
連進 Render PostgreSQL，清空舊資料
注入乾淨假資料：8分類、50客戶、100商品、323筆折扣
修正 sql.init.mode=always → never，解決啟動外鍵衝突

踩過的坑：

psql 路徑版本錯誤（裝的是 18，export 寫 17）
products INSERT 忘記 base_price 欄位
discounts INSERT 欄位名稱是 discount_ratio 不是 discount_rate
sql.init.mode=always 導致每次啟動重跑 data.sql 造成衝突

學到的概念：

psql 是 PostgreSQL 的命令列工具，\dt 看表、\d tablename 看結構
TRUNCATE ... RESTART IDENTITY CASCADE 清空並重置 ID
CROSS JOIN + WHERE RANDOM() < 0.06 隨機生成稀疏資料的技巧